define(['durandal/system', 'router','model'],
    function (system, router, model) {
        var shell = {
            activate: activate,
            router: router,
            viewAttached: viewAttached
        }
        return shell;

        function activate() {

        }

        function viewAttached() {

        }
    }
);